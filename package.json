{
  "name": "sveltekit-template",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@9.4.0",
  "scripts": {
    "build": "prisma generate && prisma db push && vite build",
    "preview": "vite preview",
    "check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
    "check:watch": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json --watch",
    "prisma:generate": "prisma generate --schema ./prisma/schema.prisma",
    "dev": "vite dev",
    "dev:migrate": "prisma migrate dev",
    "test": "pnpm test:migrate && dotenv -e .env.test -- vitest",
    "test:migrate": "dotenv -e .env.test -- prisma migrate reset -f",
    "test:coverage": "prisma migrate reset -f && vitest run --coverage",
    "postinstall": "lefthook install"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.3",
    "@sveltejs/adapter-vercel": "^5.4.4",
    "@sveltejs/kit": "^2.6.1",
    "@sveltejs/vite-plugin-svelte": "^3.1.2",
    "@testing-library/svelte": "^5.2.3",
    "@vitest/coverage-v8": "^2.1.2",
    "dotenv-cli": "^7.4.2",
    "happy-dom": "^15.7.4",
    "lefthook": "^1.7.18",
    "prisma": "^5.20.0",
    "svelte": "^4.2.19",
    "svelte-check": "^4.0.4",
    "tslib": "^2.7.0",
    "typescript": "^5.6.2",
    "vite": "^5.4.8",
    "vitest": "^2.1.2"
  },
  "dependencies": {
    "@prisma/client": "5.16.2",
    "@vercel/speed-insights": "^1.0.12",
    "ky": "^1.4.0",
    "valibot": "^0.36.0"
  }
}
